import {
  toLocalDateTime,
  calculateDurationMinutes,
  normalizeDuration,
} from '../js/components/datetime.js';

export { toLocalDateTime, calculateDurationMinutes, normalizeDuration };

export default {
  toLocalDateTime,
  calculateDurationMinutes,
  normalizeDuration,
};

